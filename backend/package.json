{
  "name": "fortress-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fortress demo backend — TypeScript + Express, JSON-on-disk persistence.",
  "main": "dist/server.js",
  "scripts": {
    "sync-codelabs": "node scripts/sync-codelabs.mjs",
    "predev": "npm run sync-codelabs",
    "dev": "tsx watch src/server.ts",
    "prebuild": "npm run sync-codelabs",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "argon2": "^0.44.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.7.0",
    "tsx": "^4.22.0",
    "typescript": "^6.0.3"
  }
}
